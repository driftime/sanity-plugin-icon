import type { ComponentProps } from "react";

/** Properties every icon the Studio draws is rendered with. */
export const defaultIconProps: ComponentProps<"svg"> = { width: "1em", height: "1em", strokeWidth: 1.5 };
